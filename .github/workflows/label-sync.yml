---
name: Label Sync

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/labels.yaml
      - .github/labels-base.yaml
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: read

jobs:
  label-sync:
    uses: DevSecNinja/.github/.github/workflows/label-sync.yml@7a3e8e1475c658c60c23bedc7daf2e559bfe60b4 # v3.1.0
    permissions:
      contents: read
      issues: write
    with:
      # This repo has no config-synced labels-base.yaml yet, so keep existing
      # (e.g. default) labels rather than pruning. Flip to true once config-sync
      # provides the shared base label set.
      delete-other-labels: false
